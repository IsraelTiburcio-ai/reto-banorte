[project]
name = "reto-ia-banorte-cv-agent"
version = "0.1.0"
description = "Conversational CV agent foundation for the Banorte AI challenge."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn>=0.30,<1",
    "openai>=1.66,<2",
]

[project.optional-dependencies]
test = [
    "httpx>=0.27,<1",
]
